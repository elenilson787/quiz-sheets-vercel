'use client';import{FormEvent,useState}from'react';
export default function Login(){const[m,setM]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),senha:f.get('senha')})});const d=await r.json();if(!d.ok)return setM(d.error);localStorage.setItem('quiz_user',JSON.stringify(d.data.user));location.href='/quiz'}return <main className="min-h-screen grid place-items-center p-6"><form onSubmit={submit} className="bg-white shadow rounded-2xl p-6 w-full max-w-md space-y-4"><h1 className="text-2xl font-bold">Entrar</h1><input name="email" type="email" required placeholder="E-mail" className="w-full border rounded-xl p-3"/><input name="senha" type="password" required placeholder="Senha" className="w-full border rounded-xl p-3"/><button className="w-full bg-blue-700 text-white rounded-xl p-3 font-semibold">Entrar</button>{m&&<p className="text-red-600 text-sm">{m}</p>}<a href="/quiz" className="block text-center text-sm text-slate-500">Continuar sem conta</a></form></main>}