'use client';import{FormEvent,useState}from'react';
export default function Register(){const[m,setM]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({nome:f.get('nome'),email:f.get('email'),senha:f.get('senha')})});const d=await r.json();setM(d.ok?'Conta criada! Agora entre.':d.error);if(d.ok)setTimeout(()=>location.href='/login',800)}return <main className="min-h-screen grid place-items-center p-6"><form onSubmit={submit} className="bg-white shadow rounded-2xl p-6 w-full max-w-md space-y-4"><h1 className="text-2xl font-bold">Criar conta</h1><input name="nome" required placeholder="Nome" className="w-full border rounded-xl p-3"/><input name="email" type="email" required placeholder="E-mail" className="w-full border rounded-xl p-3"/><input name="senha" type="password" minLength={6} required placeholder="Senha (mín. 6)" className="w-full border rounded-xl p-3"/><button className="w-full bg-blue-700 text-white rounded-xl p-3 font-semibold">Cadastrar</button>{m&&<p>{m}</p>}</form></main>}